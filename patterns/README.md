# Patterns

This directory records reusable engineering or delivery patterns proven by real Factory executions.

A pattern should describe a repeatable problem and solution without embedding product-specific business logic.

Initial known pattern to formalize later: durable remote checkpoints before independent QA to survive runtime restarts.
